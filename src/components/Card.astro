---
interface Props {
  class?: string;
}
const { class: className = "" } = Astro.props;
---

<article
  class={`rounded-2xl border border-[--color-border] bg-[--color-surface] p-6 sm:p-8 transition-colors hover:border-[--color-border-strong] ${className}`}
>
  <slot />
</article>
